disp video window set <disp_id> <in_x_pos> <in_y_pos> <out_x_pos> <out_y_pos> <in_width> <in_lines> <out_width> <out_lines>"
# Set Disp instance window setting."
#<disp_id>   Display instance ID (0~2)"
#   <in_x_pos>  Input X offset"
#   <in_y_pos>  Input Y offset"
#   <out_x_pos> Input width"
#   <out_y_pos> Input lines"
#   <in_width>  Output X offset"
#   <in_lines>  Output Y offset"
#   <out_width> Output width"
#   <out_lines> Output lines"

disp video window get <disp_id>"
# Get Disp instance window setting."
#<disp_id>   Display instance ID (0~2)"

disp video render <disp_id> <y_addr> <c_addr> <global_width>"
# Render image layer."
#<disp_id>      Display instance ID (0~2)"
#   <y_addr>       Y    data stored address (Hex)"
#   <c_addr>       CbCr data stored address (Hex)"
#   <global_width> Global input width"

disp video rotate set <disp_id> <degree>"
# Set display image layer rotation."
#<disp_id>  Display instance ID (0~2)"
#   <degree>   0:not rotate, 1:90, 2:180, 3:270"

disp video rotate get <disp_id>"
# Get display image layer rotation."
#<disp_id>  Display instance ID (0~2)"

disp video reverse set <disp_id> <h_enable> <v_enable>"
# Set display image layer reverse settings."
#<disp_id>   Display instance ID (0~2)"
#   <h_enable>  0:OFF(disable), 1:ON(enable) - Horizontal -"
#   <v_enable>  0:OFF(disable), 1:ON(enable) - Vertical -"

disp video reverse get <disp_id>"
# Get display image layer reverse settings."
#<disp_id>  Display instance ID (0~2)"

disp video mirror set <disp_id> <enable>"
# Set display image layer mirror settings."
#<disp_id>   Display instance ID (0~2)"
#   <enable>  0:OFF(disable), 1:ON(enable)"

disp video mirror get <disp_id>"
# Get display image layer mirror settings."
#<disp_id>  Display instance ID (0~2)"

disp video enable <disp_id>"
# Enable image layer."
#<disp_id>  Display instance ID (0~2)"

disp video disable <disp_id>"
# Disable image layer."
#<disp_id>  Display instance ID (0~2)"

  ---BaseFW API---"
disp video addr set <disp_id> <y_addr> <c_addr> <img format> <color_space> <global width>
# Disable image layer."
#<disp_id>       Display instance ID (0~2)"
#   <y_addr>        Y    data stored address (Hex)"
#   <c_addr>        CbCr data stored address (Hex)"
#   <img format>    0x04:420, 0x00:422,  0x0C:Ribery"
#   <color_space>   0:601, 1:709"
#   <global width>  Global width"
USAGE> disp osd config <disp_id> <layer_id> <in X pos> <in Y pos> <in Width> <in Height> <in pitch> <in format> <out X pos> <out Y pos> <out Width> <out Height> <out pitch> <out format>

# Set configulation of OSD area."
#<disp_id>   Display instance ID (0~2)"
#<layer_id>  0:OSD-0  1:OSD-1"
#<in X pos>  Input X of starting position"
#<in Y pos>  Input Y of starting position"
#<in Width>  Input width of OSD area"
#<in Height> Input height of OSD area"
#<in pitch>  Input pitch information"
#<in format>  Input format information"
#<out X pos>  Output X of starting position"
#<out Y pos>  Output Y of starting position"
#<out Width>  Output width of OSD area"
#<out Height> Output height of OSD area"
#<out pitch>  Output pitch information"
#<out format> Output format information"

disp osd copy <disp_id> <layer_id> <in X pos> <in Y pos"> <in Width> <in Height> <in pitch> <in format> <degree> <in buffer> <out buffer>"
# Copy from input buffer to output buffer."
#<disp_id>   Display instance ID (0~2)"
#<layer_id>  0:OSD-0  1:OSD-1"
#<in X pos>  Input X of starting position"
#<in Y pos>  Input Y of starting position"
#<in Width>  Input width of OSD area"
#<in Height> Input height of OSD area"
#<in pitch>  Input pitch information"
#<out X pos>  Output X of starting position"
#<out Y pos>  Output Y of starting position"
#<out Width>  Output width of OSD area"
#<out Height> Output height of OSD area"
#<out pitch>  Output pitch information"
#<degree>     Rotation Degree (clockwise)"
#<in buffer>  Input OSD buffer address"
#<ouy buffer> Output OSD buffer address"

disp osd reverse set <disp_id> <layer_id> <h_enable> <v_enable>"
# Set display image layer reverse settings."
#<disp_id>  Display instance ID (0~2)"
#<layer_id> 0:OSD-0   1:OSD-1"
#   <h_enable> 0:OFF(disable), 1:ON(enable) - Horizontal -"
#   <v_enable> 0:OFF(disable), 1:ON(enable) - Vertical -"

disp osd reverse get <disp_id> <layer_id>"
# Get display image layer reverse settings."
#<disp_id>  Display instance ID (0~2)"
#<layer_id> 0:OSD-0   1:OSD-1"

disp osd render <disp_id> <layer_id> <disp addr>"
# Copy from input buffer to output buffer."
#<disp_id>   Display instance ID (0~2)"
#<layer_id>  0:OSD-0   1:OSD-1"
#<disp addr> Display OSD buffer address"

disp osd en <disp_id> <layer_id> "
# Enable OSD layer."
#<disp_id>  Display instance ID (0~2)"
#<layer_id> 0:OSD-0   1:OSD-1"

disp osd dis <disp_id> <layer_id> "
# Disable OSD layer."
#<disp_id>  Display instance ID (0~2)"
#<layer_id> 0:OSD-0   1:OSD-1"

disp osd pallet state set <disp_id> <layer_id> <state>"
# Set OSD Pallet Table state."
#<disp_id>  Display instance ID (0~2)"
#<layer_id> 0:OSD-0   1:OSD-1"
#   <state>    0:Normal  1:Sleep    2:Shutdown"

disp osd pallet state get <disp_id> <layer_id>"
# Get OSD Pallet Table state."
#<disp_id>  Display instance ID (0~2)"
#<layer_id> 0:OSD-0   1:OSD-1"

disp osd pallet config <disp_id> <layer_id> <area> <format>"
# Set OSD Pallet Table configuration."
#<disp_id>  Display instance ID (0~2)"
#<layer_id> 0:OSD-0    1:OSD-1"
#   <area>     0:A area   1:B area"
#   <format>   0:RGBA8888 1:YCbCrA4448"

disp osd pallet table set <disp_id> <layer_id> <area>"
# Set OSD Pallet Table data."
#<disp_id>  Display instance ID (0~2)"
#<layer_id> 0:OSD-0    1:OSD-1"
#   <area>     0:A area   1:B area"
